/**
 * 顺序查找
 * @param {参数} arrayParam 
 * @param {要查找的目标} aim 
 */
function orderSearch(arrayParam, aim) {
  for (let i=0; i< arrayParam.length; i++){
    if (arrayParam[i] == item){
      return id;
    }
  }

  return -1;
}